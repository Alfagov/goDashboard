package templates

templ IndexPage(body templ.Component) {
    @header()
    <body class="bg-gray-100 h-screen font-sans flex flex-col">
		<div class="container mx-auto p-4 h-full">
			@NavbarWidget()
            {! body }
        </div>
    </body>

}